--- 
apiVersion: v1
kind: Pod
metadata: 
  name: container-type-pod
  labels:
    app: container-type-pod
    purpose: "Eample of container type pod"
spec:
  initContainers:
    - name: initcon-1
      image: alpine:1.25
      command:
        - sleep
        - 1m
    - name: initcon-2
      image: alpine:1.25
      command:
        - sleep
        - 1m
  containers:
    - name: webnginx-server
      image: nginx:latest
      ports:
        - containerPort: 80
          protocol: TCP
